# Human-reviewed derivatives

Only schema-valid derivatives with complete provenance belong here. Each child
directory has a distinct schema and validator: `events/` is for review work
products and `precipitation/` is for canonical precipitation observations.
Neither is implicitly a canonical flood-label row. Review does not itself
authorize training; `training-authorization.json` remains decisive.
