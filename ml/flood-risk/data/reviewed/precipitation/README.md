# Canonical precipitation observations

Derived records must conform to `precipitation-observation.schema.json` and
retain raw file references, checksums, native values/units, conversion method,
grid identity, product version, and source resolution. Missing values stay
missing and exclude a candidate window.
