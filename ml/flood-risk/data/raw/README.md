# Immutable raw data

Store acquired source files only in the appropriate child directory. Never edit
them in place. Record the source registry entry, retrieval timestamp, original
filename, and SHA-256 checksum before creating a reviewed derivative.
